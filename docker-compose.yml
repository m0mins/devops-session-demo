version: '3.8'

services:
  app:
    build: .
    container_name: flask_app
    ports:
      - "80:80"
      # - "5000:5000"
    restart: always
    networks:
      - demo-net

networks:
  demo-net:
    driver: bridge
